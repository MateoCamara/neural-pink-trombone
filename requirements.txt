# Curated, cross-platform direct dependencies for neural-pink-trombone.
#
# The original file was a full `pip freeze` of the (Linux + CUDA) training
# environment; it pinned platform-specific `nvidia-*-cu12` and `triton` wheels
# that make `pip install -r` fail on Windows/macOS/CPU. That exact freeze is
# preserved verbatim in `requirements-freeze.txt` for byte-for-byte reproduction
# on the original platform; this file lists only the packages the code imports
# and lets pip resolve the CUDA / transitive wheels for your platform.
#
# For a GPU build, install the torch wheels matching your CUDA version from
# https://pytorch.org/get-started/locally/ before installing the rest.

# --- Deep learning ---
torch==2.2.2
torchaudio==2.2.2
torchvision==0.17.2
lightning==2.2.1
transformers==4.39.3
datasets==2.18.0
encodec==0.1.1

# --- Audio / signal processing ---
librosa==0.10.1
soundfile==0.12.1
sounddevice==0.4.6
scipy==1.12.0

# --- Scientific / data ---
numpy==1.26.4
pandas==2.2.1
scikit-learn==1.4.1.post1

# --- Plotting ---
matplotlib==3.8.3
seaborn==0.13.2          # used by visualizations/ (was missing from the freeze)

# --- Logging / utils ---
tensorboard==2.16.2
requests==2.31.0
tqdm==4.66.2
PyYAML==6.0.1

# --- Optional: live-microphone visualization only ---
# `keyboard` captures the spacebar in visualizations/generate_audio_from_microphone.py.
# It was missing from the freeze and may require elevated privileges on Linux/macOS.
keyboard==0.13.5
